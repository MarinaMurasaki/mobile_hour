<!DOCTYPE html>
<html lang="en">
    <head>
        <link rel="stylesheet" href="/style.css">
        <title>Mobile Hour - Checkout</title>
    </head>
    <body>
        <main>
            <!-- TODO: Header / nav -->
            <%- include("partials/header.ejs") %>
            <section class="checkout-layout">
                <article class="phone-card">
                    <img src="/image/phone.png"
                        alt="phone" height="300px" >
                    <h2><%= product.name %></h2>
                    <span>$<%= product.price %></span>
                    <p><%= product.description %></p>
                </article>
                <form action="/create_order" method="post"
                    class="form-grid">
                    <input type="hidden" name="product_id"
                        value="<%= product.id %>">
                    <label for="customer-first-name">First
                        Name:</label>
                    <input type="text" name="customer_first_name"
                        id="customer-first-name">
                    <label for="customer-last-name">Last Name:</label>
                    <input type="text" name="customer_last_name"
                        id="customer-last-name">
                    <label for="customer-phone">Phone:</label>
                    <input type="tel" name="customer_phone"
                        id="customer-phone">
                    <label for="customer-email">Email:</label>
                    <input type="email" name="customer_email"
                        id="customer-email">
                    <input type="submit" value="Pay" class="pay">
                </form>

                <a href="/" class="link-button">Back</a>

            </section>
            <!-- TODO: Footer -->
            <%- include("partials/footer.ejs") %>
        </main>
    </body>
</html>